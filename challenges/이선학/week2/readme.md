c1 
C언어는 각 자료형의 최소 크기를 규정하고 있음
Signed 과 Unsigned 차이점 : 부호 여부와 상관없이 저장 공간의 크기는 동일하지만 부호 비트를 숫자를 표현하는데에 쓰는지, 부호를 표현하는데 쓰는지의 차이가 있음

c3
부호가 있는 정수 : 8비트 기준_ 최댓값 01111111에 1을 더하면 10000000이 됨 = 음수 최솟값
부호가 없는 정수 : 11111111에서 1을 더하면 모든 비트가 00000000으로 바뀌고 비트를 벗어난 수는 사라짐 = 0
